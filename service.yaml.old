apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: myapp
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: gcr.io/useful-aquifer-302901/myapp:tag1